export const metadata = { title: 'Tax - Root OS' }

export default function TaxPage() {
  return (
    <main style={{ padding: '1.5rem', maxWidth: '800px', margin: '0 auto' }}>
      <h1 style={{ color: 'var(--text)', fontSize: '20px', fontWeight: 600, marginBottom: '0.5rem' }}>
        Tax
      </h1>
      <p style={{ color: 'var(--text-muted)', fontSize: '14px' }}>
        Tax reporting features are coming soon. Use the Transactions view to export data for now.
      </p>
    </main>
  )
}
